# vendor-web

Vendor Portal scaffold. Feature implementation is pending a future sprint — this
is an empty Next.js shell wired into the monorepo (shared packages, lint config,
and the `features/` convention are already in place; see `features/README.md`).
